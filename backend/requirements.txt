# Lower bounds only: the prototype targets a recent Python (3.13+) where exact
# legacy pins lack wheels. Pip resolves the newest compatible releases.

# --- Web API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5
httpx>=0.27              # used by fastapi TestClient

# --- Data / ML (used from Level 1 onward) ---
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
joblib>=1.3             # model persistence (Phase 6); ships with scikit-learn
pyarrow>=17            # parquet I/O
networkx>=3.3

# --- Dev / test ---
pytest>=8.3
ruff>=0.6

# Frontend

React + TypeScript dashboard for SideEffect2Cure AI.

**Not yet scaffolded.** Level 0 only reserves this directory. The dashboard
(Vite + React + TypeScript) is initialized in a later level once the backend
pipeline exposes data to display.
